<!--
Thanks for this. Three short answers below are enough; delete anything that does
not apply.
-->

### What this changes, and why

<!-- The why is the part the diff cannot show. -->

### The check that fails without it

<!-- Name the test, or say why the change needs none (docs, typo, link). -->

### What it costs

<!-- New dependency, slower run, more memory, behaviour that differs in a case
that used to work. "Nothing" is a valid answer if it is true. -->

---

- [ ] Tests pass locally (`cargo test` / `--selftest` / `npm test`)
- [ ] An `Honest limits` entry this change invalidates has been updated
